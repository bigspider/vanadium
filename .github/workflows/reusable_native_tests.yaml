name: Reusable V-App Unit Tests

on:
  workflow_call:
    inputs:
      vapp_name:
        description: "Human readable V-App name (e.g. vnd-bitcoin)"
        required: true
        type: string
      test_dirs_json:
        description: "JSON array of relative directories to run cargo test in (e.g. ['apps/bitcoin/common','apps/bitcoin/app'])"
        required: true
        type: string
      rust_toolchain:
        description: "Rust toolchain"
        required: false
        default: nightly
        type: string
      rust_target:
        description: "Native target triple"
        required: false
        default: x86_64-unknown-linux-gnu
        type: string
      cargo_test_args:
        description: "Extra cargo test args"
        required: false
        default: ""
        type: string

jobs:
  unit-tests:
    name: Unit tests (${{ inputs.vapp_name }})
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        test_dir: ${{ fromJson(inputs.test_dirs_json) }}
    steps:
      - name: Set up Rust toolchain
        uses: dtolnay/rust-toolchain@master
        with:
          toolchain: ${{ inputs.rust_toolchain }}
          targets: ${{ inputs.rust_target }}
          components: rustfmt, clippy

      - name: Cache cargo dirs
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            target
          key: ${{ runner.os }}-cargo-${{ inputs.rust_toolchain }}-${{ hashFiles('**/Cargo.lock') }}
          restore-keys: |
            ${{ runner.os }}-cargo-${{ inputs.rust_toolchain }}-

      - name: Install system deps (udev, pkg-config, WebKitGTK)
        # WebKitGTK (and GTK 3) are needed by V-Apps whose native build enables the
        # `native-window` feature by default (e.g. vnd-test, vnd-bitcoin).
        run: sudo apt-get update && sudo apt-get install -y libudev-dev pkg-config libgtk-3-dev libwebkit2gtk-4.1-dev

      - name: Checkout sources
        uses: actions/checkout@v4

      - name: Cargo test (${{ matrix.test_dir }})
        working-directory: ${{ matrix.test_dir }}
        run: |
          cargo +${{ inputs.rust_toolchain }} test --target ${{ inputs.rust_target }} ${{ inputs.cargo_test_args }}
